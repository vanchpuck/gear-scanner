//package org.izolotov.crawler.timeoutscraper
//
//class HostQueueFetcher[Response](/*link to shared queue*/) extends BaseFetcher[Response] {
//  override def fetch(url: String): Response = {
////    TODO use shared queue for fetching ordering
//
////    фетчер возвращает респонс
////    скрапер возвращает аттемпт(конкретный набор нужных полей + метаданные)
////    скраперы делят единую очередь
////    хттп скрапер возвращает [ClseableHttpResponse]
////    селениум скрапер возвращает [String]
//  }
//}
